[project]
name = "nested-repo-2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "dspy",
]

[tool.uv.dependencies]
modaic = { path = "../../../../", develop = true }
