---
title: "Initialize Arbiter"
api: "POST /api/v1/arbiters"
---

Initialize a new arbiter repository.

<ParamField body="repo" type="string" required>
  Target arbiter repository.
</ParamField>

<ParamField body="inputs" type="FieldSchema[]" required>
  Input field definitions.
  <Expandable title="FieldSchema properties">
    <ParamField body="inputs[].name" type="string" required>
      Field name.
    </ParamField>
    <ParamField body="inputs[].type" type="string" required>
      Enum: `string`, `number`, `boolean`, `array`, `object`.
    </ParamField>
    <ParamField body="inputs[].allowed_values" type="object[] | null">
      Optional allowed values.
    </ParamField>
    <ParamField body="inputs[].object_schema" type="object | null">
      Nested schema for structured fields.
    </ParamField>
    <ParamField body="inputs[].nullable" type="boolean" default="false">
      Whether the field is nullable.
    </ParamField>
    <ParamField body="inputs[].description" type="string | null">
      Field description.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="outputs" type="FieldSchema[]" required>
  Output field definitions. Same shape as items in `inputs`.
  <Expandable title="FieldSchema properties">
    <ParamField body="outputs[].name" type="string" required>
      Field name.
    </ParamField>
    <ParamField body="outputs[].type" type="string" required>
      Enum: `string`, `number`, `boolean`, `array`, `object`.
    </ParamField>
    <ParamField body="outputs[].allowed_values" type="object[] | null">
      Optional allowed values.
    </ParamField>
    <ParamField body="outputs[].object_schema" type="object | null">
      Nested schema for structured fields.
    </ParamField>
    <ParamField body="outputs[].nullable" type="boolean" default="false">
      Whether the field is nullable.
    </ParamField>
    <ParamField body="outputs[].description" type="string | null">
      Field description.
    </ParamField>
  </Expandable>
</ParamField>

<ParamField body="instructions" type="string | null">
  Optional arbiter instructions.
</ParamField>

<ParamField body="model" type="string" default="qwen3-vl-32b-instruct">
  Model identifier.
</ParamField>

<ParamField body="base_url" type="string | null">
  Optional provider base URL override.
</ParamField>
