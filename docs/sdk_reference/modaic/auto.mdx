---
title: Auto Classes
sidebarTitle: Auto
description: Dynamic loading helpers for configs and programs.
---

## AutoConfig

```python
class AutoConfig()
```

#### `from_precompiled`

```python
@staticmethod
def from_precompiled(repo_path: str, rev: str = "main", **kwargs) -> PrecompiledConfig
```

Loads `config.json` from a local folder or Modaic Hub repo and applies keyword overrides.

## AutoProgram

```python
class AutoProgram()
```

#### `from_precompiled`

```python
@staticmethod
def from_precompiled(
    repo_path: str,
    *,
    access_token: Optional[str] = None,
    config: Optional[dict] = None,
    rev: str = "main",
    **kw,
) -> PrecompiledProgram
```

Loads a bundled program dynamically from a local folder or Modaic Hub repo.

## Registration helpers

#### `builtin_program`

```python
def builtin_program(name: str) -> Callable[[Type], Type]
```

Decorator used to register a built-in program class.

#### `builtin_config`

```python
def builtin_config(name: str) -> Callable[[Type], Type]
```

Decorator used to register a built-in config class.