---
title: Program Card
description: The program card is the main documentation surface for a program repository.
sidebarTitle: Program Card
---

The **program card** is the main overview page for a repository on Modaic Hub.

On the Hub, the program card is rendered from the repository's root `README.md`.

![Program Card](https://d1pz4mbco29rws.cloudfront.net/public/program-card-example.png)

## What the program card is for

Use the program card to answer the questions a collaborator or downstream user will have first:

- What does this program do?
- What task is it intended for?
- How should it be loaded or run?
- What assumptions, limits, or dependencies matter?

## README-based rendering

If a repository has a root-level `README.md`, Modaic renders that file as the program card.

If there is no `README.md`, the program card has no main content yet and should be created as part of the initial repository setup.

## Frontmatter and metadata

`README.md` can include YAML frontmatter at the top of the file:

```mdx
---
is_arbiter: true
confidence_threshold: 0.7
slack_notifications: true
---

# Support Triage Arbiter

This program classifies incoming support tickets by priority.
```

For arbiter repositories, frontmatter is used for repository-level behavior such as:

- `is_arbiter`
- `confidence_threshold`
- `slack_notifications`

![Program Card Metadata](https://d1pz4mbco29rws.cloudfront.net/public/program-card-metadata.png)

The Markdown body after the frontmatter is the main human-readable content of the card.

## Structured program metadata

In addition to `README.md`, a program repository also includes structured runtime artifacts such as `program.json` and `config.json`.

These files are not the program card itself. Instead:

- `README.md` is the human-facing documentation surface
- `program.json` is the serialized DSPy program artifact
- `config.json` is the serialized environment, parameter, and signature state

When available, Modaic Hub can also surface information derived from `program.json`, such as module definitions and dependency versions.

## What to include

A strong program card usually includes:

- A one-paragraph summary
- The input and output shape of the program
- Loading or usage examples
- A short note describing the required repository artifacts
- Notes about dependencies or external services
- Limits, edge cases, or evaluation context

Continue to [Files and Versions](/hub/programs/files_and_versions).
